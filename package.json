{
  "name": "cf-secure-payment-form",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "format": "prettier --write src"
  },
  "dependencies": {
    "hono": "^4.6.9",
    "stripe": "^17.3.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "prettier": "^3.3.3",
    "wrangler": "^3.83.0"
  }
}
